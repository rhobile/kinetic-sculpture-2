rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Helper function to check for administrative access
    function isAdmin() {
      return request.auth != null && (
        request.auth.uid == 'ge6KSJEZKFXsNZerEbXseOR2vSS2' || 
        request.auth.token.email == 'rhobile@gmail.com' ||
        // Allow the current anonymous session for development convenience
        request.auth.uid == 'yuFerZOVxgXYUM4GRhlXBrxCSNt1'
      );
    }

    // Explicit rules for primary collections
    match /videos/{videoId} {
      allow read, list: if true;
      allow write: if isAdmin();
    }
    
    match /news/{newsId} {
      allow read, list: if true;
      allow write: if isAdmin();
    }
    
    match /observations/{obsId} {
      allow read, list: if true;
      allow write: if isAdmin();
    }
    
    match /pages/{pageId} {
      allow read, list: if true;
      allow write: if isAdmin();
    }

    // Global fallback for queries and subcollections
    match /{document=**} {
      allow read, list: if true;
      allow write: if isAdmin();
    }
    
    // Deployment trigger version: 2.5.0
  }
}